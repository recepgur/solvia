# Solvio Blockchain

A specialized blockchain network optimized for decentralized communication.

## Development Setup

1. Ensure Python 3.12 is installed
2. Install dependencies: `poetry install`
3. Run tests: `poetry run pytest`
