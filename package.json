{
  "name": "solvia",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "pnpm --filter @solvia/messenger dev",
    "build": "pnpm --filter @solvia/messenger build",
    "lint": "pnpm --filter @solvia/messenger lint",
    "clean": "pnpm --filter @solvia/messenger clean"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "prettier": "^3.2.4",
    "eslint": "^8.56.0"
  }
}
