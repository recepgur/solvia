"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.fixEscapeCharactersInRawEnvValue = void 0;
function fixEscapeCharactersInRawEnvValue(rawValue) {
    const escapeCharsMatches = rawValue.match(/\\(\w)/g);
    let updatedValue = rawValue;
    for (const match of escapeCharsMatches !== null && escapeCharsMatches !== void 0 ? escapeCharsMatches : []) {
        if (match[1] === 'n') {
            updatedValue = updatedValue.replace(`${match[0]}${match[1]}`, '\n');
        }
        if (match[1] === 't') {
            updatedValue = updatedValue.replace(`${match[0]}${match[1]}`, '\t');
        }
        if (match[1] === 'r') {
            updatedValue = updatedValue.replace(`${match[0]}${match[1]}`, '\r');
        }
    }
    return updatedValue;
}
exports.fixEscapeCharactersInRawEnvValue = fixEscapeCharactersInRawEnvValue;
//# sourceMappingURL=envUtils.js.map