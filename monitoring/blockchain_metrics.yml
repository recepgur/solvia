# Blockchain metrics configuration
scrape_configs:
  - job_name: 'blockchain_metrics'
    static_configs:
      - targets: ['localhost:8545']  # Ethereum RPC
      - targets: ['localhost:8899']  # Solana RPC
    metrics_path: '/metrics'
    scheme: 'http'
    scrape_interval: 15s
